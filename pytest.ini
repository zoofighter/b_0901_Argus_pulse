[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    llm: LLM API 호출이 필요한 테스트 (GEMINI_API_KEY 필요)
    no_llm: LLM 없이 실행 가능한 테스트
    slow: 실행 시간이 긴 테스트

addopts = -v --tb=short
